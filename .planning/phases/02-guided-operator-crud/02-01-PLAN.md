---
phase: 02-guided-operator-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/manage-person/SKILL.md
  - .agents/skills/manage-person/agents/openai.yaml
  - package.json
  - README.md
  - scripts/manage-person.ts
  - scripts/lib/manage-person/action-contract.ts
  - scripts/lib/manage-person/person-registry.ts
  - scripts/manage-person.test.ts
autonomous: true
requirements:
  - OPER-01
user_setup: []
must_haves:
  truths:
    - The repo exposes one primary `manage-person` operator surface whose actions match the Phase 2 context decisions.
    - The underlying CLI contract is explicit and deterministic so the project skill can drive it reliably.
    - Existing people can be loaded and searched by id or display name through a shared registry layer instead of ad hoc directory scans.
  artifacts:
    - .agents/skills/manage-person/SKILL.md
    - scripts/manage-person.ts
    - scripts/lib/manage-person/action-contract.ts
    - scripts/lib/manage-person/person-registry.ts
  key_links:
    - .agents/skills/manage-person/SKILL.md defines the repo-local conversational CRUD workflow that becomes the primary operator surface.
    - scripts/manage-person.ts is the single CLI entrypoint underneath the skill, with explicit action routing.
    - scripts/lib/manage-person/person-registry.ts centralizes searchable person loading for later create/update/disable/archive actions.
---

<objective>
Establish the operator-facing command and skill surface for Guided Operator CRUD.

Purpose: Give later create/update/disable/archive plans one stable entrypoint and person-selection layer, without yet implementing all mutations.
Output: A project skill under `.agents/skills/`, a top-level `manage-person` CLI router, and parsed person-registry helpers that future actions can reuse.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-guided-operator-crud/02-CONTEXT.md
@.planning/phases/02-guided-operator-crud/02-RESEARCH.md
@.planning/phases/01-foundation-and-data-contracts/01-03-SUMMARY.md
@scripts/scaffold-person.ts
@scripts/validate.ts
@/Users/peterryszkiewicz/.codex/skills/.system/skill-creator/SKILL.md
@/Users/peterryszkiewicz/Repos/open-links/docs/openclaw-update-crud.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the project skill and operator surface contract</name>
  <files>.agents/skills/manage-person/SKILL.md, .agents/skills/manage-person/agents/openai.yaml, README.md</files>
  <action>Create the repo-contained `manage-person` project skill that acts as the preferred operator workflow in v1. It should define the explicit action menu (`create`, `update`, `disable`, `archive`), emphasize minimal conversational prompting, and describe how the skill drives the underlying CLI instead of editing files manually. Add only the skill metadata/resources needed for this repo-local workflow.</action>
  <verify>bun run typecheck</verify>
  <done>The repo has one documented project skill that matches the Phase 2 context decisions and points to one underlying CRUD command surface.</done>
</task>

<task type="auto">
  <name>Task 2: Build the shared CLI router and person-registry primitives</name>
  <files>package.json, scripts/manage-person.ts, scripts/lib/manage-person/action-contract.ts, scripts/lib/manage-person/person-registry.ts</files>
  <action>Introduce one top-level `manage-person` CLI entrypoint with explicit action parsing and room for later action modules. Add a parsed person-registry helper that loads `person.json` metadata, supports lookup by id or display name, and establishes the default filtering behavior later actions will use. Keep this layer deterministic and noninteractive so the skill can drive it safely.</action>
  <verify>bun test scripts/manage-person.test.ts --filter surface</verify>
  <done>Later plans can plug create/update/disable/archive mutations into a stable CLI and searchable person-registry layer instead of inventing new entrypoints.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the operator surface with tests and usage docs</name>
  <files>scripts/manage-person.test.ts, README.md, .agents/skills/manage-person/SKILL.md</files>
  <action>Add focused tests that prove the action contract, registry lookup behavior, and default operator routing all match the locked Phase 2 decisions. Update docs so the repo makes it obvious that `manage-person` is now the preferred CRUD entrypoint over raw JSON editing.</action>
  <verify>bun test scripts/manage-person.test.ts && bun run typecheck</verify>
  <done>The Phase 2 operator surface is explicit, tested, and ready for create/update implementation in the next plan.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun test scripts/manage-person.test.ts` passes
- [ ] `bun run typecheck` passes
- [ ] The skill, CLI router, and tests all agree on the same explicit action set
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has one project-skill-first CRUD surface and one reusable person-registry abstraction for later plans
</success_criteria>

<output>
After completion, create `.planning/phases/02-guided-operator-crud/02-01-SUMMARY.md`
</output>
