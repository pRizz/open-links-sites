---
phase: 02-guided-operator-crud
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - package.json
  - README.md
  - scripts/manage-person.ts
  - scripts/lib/manage-person/create-person.ts
  - scripts/lib/manage-person/update-person.ts
  - scripts/lib/manage-person/mutation-session.ts
  - scripts/lib/manage-person/update-tasks.ts
  - scripts/manage-person.test.ts
autonomous: true
requirements:
  - OPER-01
  - OPER-03
user_setup: []
must_haves:
  truths:
    - The operator can create a person from the primary CRUD surface by providing only a name, plus an optional seed URL.
    - The operator can select an existing person by name or id and apply task-based profile/site/orchestration updates without hand-editing JSON.
    - Create and update both write changes, rerun validation, and surface remediation-friendly summaries while restoring prior state on blocking failures.
  artifacts:
    - scripts/manage-person.ts
    - scripts/lib/manage-person/create-person.ts
    - scripts/lib/manage-person/update-person.ts
    - scripts/lib/manage-person/mutation-session.ts
    - scripts/lib/manage-person/update-tasks.ts
  key_links:
    - scripts/lib/manage-person/create-person.ts composes the Phase 1 scaffold and validation primitives into the low-friction create flow.
    - scripts/lib/manage-person/update-person.ts applies task-based edits after confirming the intended person match from the registry.
    - scripts/lib/manage-person/mutation-session.ts provides the rollback-safe write path required by the write-first Phase 2 workflow.
---

<objective>
Implement the guided create and update workflows on top of the validated person model.

Purpose: Turn the Phase 2 surface into a working operator CRUD flow for the most common day-2 actions.
Output: `create` and `update` actions, scoped update-task handling, and transactional mutation helpers that keep the repo valid when writes fail.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-guided-operator-crud/02-CONTEXT.md
@.planning/phases/02-guided-operator-crud/02-RESEARCH.md
@.planning/phases/02-guided-operator-crud/02-01-SUMMARY.md
@scripts/scaffold-person.ts
@scripts/validate.ts
@scripts/lib/fill-templates.ts
@scripts/lib/validate-person.ts
@/Users/peterryszkiewicz/Repos/open-links/docs/ai-guided-customization.md
@/Users/peterryszkiewicz/Repos/open-links/docs/openclaw-update-crud.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the low-friction create action</name>
  <files>scripts/manage-person.ts, scripts/lib/manage-person/create-person.ts, package.json</files>
  <action>Build the `create` action so the underlying CLI can create a person from only a name plus an optional seed URL. Generate the stable id automatically, reuse the Phase 1 scaffold helper instead of writing files manually, and return a short post-write summary including validation warnings/suggestions when the create succeeds. Keep the flow write-first and fail fast on blocking problems.</action>
  <verify>bun test scripts/manage-person.test.ts --filter create</verify>
  <done>The repo has a working create action beneath the primary skill surface, and that action matches the user’s low-friction Phase 2 decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement task-based update actions with rollback-safe writes</name>
  <files>scripts/manage-person.ts, scripts/lib/manage-person/update-person.ts, scripts/lib/manage-person/mutation-session.ts, scripts/lib/manage-person/update-tasks.ts</files>
  <action>Implement the `update` action so it can locate people by name or id, confirm the intended match, and apply tight-scope task-based changes for common profile, site, and orchestration edits. Add a small shared mutation-session helper that snapshots touched files, reruns validation after changes, and restores prior content when blocking validation errors occur.</action>
  <verify>bun test scripts/manage-person.test.ts --filter update</verify>
  <done>Common profile/site/orchestration edits can be performed through one guided CRUD surface without leaving the repo in a broken intermediate state.</done>
</task>

<task type="auto">
  <name>Task 3: Cover create/update behavior end-to-end and document the flow</name>
  <files>scripts/manage-person.test.ts, README.md, .agents/skills/manage-person/SKILL.md</files>
  <action>Add realistic tests that exercise create and update through the CLI contract the skill will call. Cover auto-generated ids, optional seed URL handling, non-blocking remediation summaries, scoped updates, and rollback on blocking failures. Update docs and skill guidance so create/update are clearly the preferred operator path for common edits.</action>
  <verify>bun test scripts/manage-person.test.ts && bun run typecheck</verify>
  <done>Create and update are regression-tested and documented before the status-changing lifecycle actions land.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun test scripts/manage-person.test.ts` passes
- [ ] `bun run typecheck` passes
- [ ] The create action works from only `name` plus optional seed URL
- [ ] Update summaries distinguish blocking problems from non-blocking remediation guidance
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo can create and update people through one guided operator surface without manual JSON editing
</success_criteria>

<output>
After completion, create `.planning/phases/02-guided-operator-crud/02-02-SUMMARY.md`
</output>
