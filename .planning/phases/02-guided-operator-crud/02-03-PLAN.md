---
phase: 02-guided-operator-crud
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - schemas/person.schema.json
  - templates/default/person.json
  - scripts/lib/validate-person.ts
  - scripts/lib/manage-person/person-registry.ts
  - scripts/lib/manage-person/status-person.ts
  - scripts/manage-person.ts
  - scripts/manage-person.test.ts
  - README.md
  - .agents/skills/manage-person/SKILL.md
autonomous: true
requirements:
  - OPER-04
user_setup: []
must_haves:
  truths:
    - Disable and archive are metadata state changes that preserve the full person folder in source control.
    - Archived people are hidden by default in normal operator flows but can still be retrieved explicitly when needed.
    - Disable and archive require one explicit confirmation before write and are covered by regression tests across the CRUD surface.
  artifacts:
    - schemas/person.schema.json
    - scripts/lib/validate-person.ts
    - scripts/lib/manage-person/status-person.ts
    - scripts/manage-person.ts
    - scripts/manage-person.test.ts
  key_links:
    - schemas/person.schema.json and scripts/lib/validate-person.ts together codify the lifecycle metadata shape for disabled and archived people.
    - scripts/lib/manage-person/status-person.ts implements the state transitions while preserving source folders.
    - scripts/lib/manage-person/person-registry.ts enforces the hidden-by-default archived lookup behavior used by the operator surface.
---

<objective>
Implement disable/archive handling and lock down regression coverage for the Guided Operator CRUD surface.

Purpose: Finish the Phase 2 CRUD contract by formalizing lifecycle metadata and status-changing actions without deleting source data.
Output: Disable/archive actions, archived visibility rules, updated validation/schema coverage, and end-to-end CRUD regression tests/docs.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-guided-operator-crud/02-CONTEXT.md
@.planning/phases/02-guided-operator-crud/02-RESEARCH.md
@.planning/phases/02-guided-operator-crud/02-01-SUMMARY.md
@.planning/phases/02-guided-operator-crud/02-02-SUMMARY.md
@schemas/person.schema.json
@templates/default/person.json
@scripts/lib/validate-person.ts
@scripts/validate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Codify lifecycle metadata and validation rules for disable/archive</name>
  <files>schemas/person.schema.json, templates/default/person.json, scripts/lib/validate-person.ts</files>
  <action>Extend the repo-local person metadata shape to support the lifecycle semantics Phase 2 needs while keeping Phase 1 data valid. Disable should remain a build/deploy exclusion concept, while archive should become an explicit metadata state with enough information for operator flows and later automation. Update validation so these new semantics are checked consistently.</action>
  <verify>bun test scripts/manage-person.test.ts --filter lifecycle-schema</verify>
  <done>The data contract explicitly represents disabled and archived people, and validation understands those lifecycle states.</done>
</task>

<task type="auto">
  <name>Task 2: Implement disable/archive actions and hidden-by-default archive behavior</name>
  <files>scripts/lib/manage-person/person-registry.ts, scripts/lib/manage-person/status-person.ts, scripts/manage-person.ts, .agents/skills/manage-person/SKILL.md</files>
  <action>Implement `disable` and `archive` actions beneath the primary CRUD surface. Require one explicit confirmation flag before write, preserve source folders in place, and make archived people hidden by default in normal lookup flows while still retrievable explicitly for archive-aware operations. Keep status changes rollback-safe by reusing the mutation-session pattern from the prior plan.</action>
  <verify>bun test scripts/manage-person.test.ts --filter lifecycle-actions</verify>
  <done>The operator can disable or archive people through metadata state changes without deleting source data or exposing archived entries in normal flows.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the full CRUD surface and update repo guidance</name>
  <files>scripts/manage-person.test.ts, README.md, .agents/skills/manage-person/SKILL.md</files>
  <action>Add regression coverage for the full Phase 2 surface: create, update, disable, and archive. Cover hidden-by-default archived behavior, explicit archive retrieval, confirmation-gated status changes, and compatibility with validation. Update docs and skill guidance so the Phase 2 CRUD workflow is clear before Phase 3 import/enrichment builds on it.</action>
  <verify>bun test scripts/manage-person.test.ts && bun run typecheck && bun run validate</verify>
  <done>The Phase 2 CRUD surface is stable, documented, and safe to build on in later phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun test scripts/manage-person.test.ts` passes
- [ ] `bun run typecheck` passes
- [ ] `bun run validate` still passes for the repo
- [ ] Archived people are hidden by default but retrievable explicitly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo supports guided create, update, disable, and archive flows without manual JSON editing or source-data deletion
</success_criteria>

<output>
After completion, create `.planning/phases/02-guided-operator-crud/02-03-SUMMARY.md`
</output>
