---
phase: 05-autonomous-upstream-sync-and-release-ops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - README.md
  - config/upstream-open-links.json
  - scripts/sync-upstream.ts
  - scripts/sync-upstream.test.ts
  - scripts/lib/release-ops/upstream-state.ts
  - scripts/lib/release-ops/upstream-sync.ts
  - scripts/lib/release-ops/release-summary.ts
  - .github/workflows/upstream-sync.yml
autonomous: true
requirements:
  - AUTO-01
user_setup: []
must_haves:
  truths:
    - This repo tracks a source-controlled upstream `open-links` revision instead of relying on a floating CI checkout.
    - Daily sync no-ops cleanly when upstream has not moved and publishes one consolidated sync commit when it has.
    - No automated write reaches `main` unless verification passes first.
  artifacts:
    - config/upstream-open-links.json
    - scripts/sync-upstream.ts
    - scripts/lib/release-ops/upstream-state.ts
    - scripts/lib/release-ops/upstream-sync.ts
    - .github/workflows/upstream-sync.yml
  key_links:
    - config/upstream-open-links.json stores the pinned upstream repo, branch, and commit used by release operations.
    - scripts/lib/release-ops/upstream-sync.ts compares tracked state to the latest upstream state and decides no-op vs verify-and-publish.
    - .github/workflows/upstream-sync.yml schedules the daily sync and only pushes after verification succeeds.
---

<objective>
Implement the daily upstream sync contract for Phase 5.

Purpose: Make upstream `open-links` movement a first-class, source-controlled change in this repo instead of a floating CI assumption.
Output: A tracked upstream state file, a sync CLI that can no-op or prepare a verified sync update, and a daily direct-to-`main` workflow that only publishes one consolidated commit after verification passes.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-autonomous-upstream-sync-and-release-ops/05-CONTEXT.md
@.planning/phases/05-autonomous-upstream-sync-and-release-ops/05-RESEARCH.md
@.github/workflows/deploy.yml
@scripts/lib/build/upstream-site-builder.ts
@scripts/lib/import/upstream-open-links-runner.ts
@/Users/peterryszkiewicz/Repos/open-links/.github/workflows/nightly-follower-history.yml
@/Users/peterryszkiewicz/Repos/open-links/scripts/github-actions/nightly-follower-history.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tracked upstream state and sync decision logic</name>
  <files>config/upstream-open-links.json, scripts/lib/release-ops/upstream-state.ts, scripts/lib/release-ops/upstream-sync.ts, scripts/sync-upstream.test.ts</files>
  <action>Add a source-controlled upstream state file plus helpers that can read/write it, inspect the checked-out upstream repo, and classify sync runs as `no-op`, `changed`, or `blocked`. Keep the contract explicit: repository, branch, tracked commit, and last sync timestamp. The helper should prepare a concise stage-based summary suitable for GitHub Actions output.</action>
  <verify>bun test scripts/sync-upstream.test.ts && bun run typecheck</verify>
  <done>The repo has a deterministic local contract for “which upstream `open-links` revision are we synced to?” and can tell whether a new daily sync should do anything.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the `sync-upstream` CLI with verification gating</name>
  <files>package.json, scripts/sync-upstream.ts, scripts/lib/release-ops/upstream-sync.ts, scripts/lib/release-ops/release-summary.ts, scripts/sync-upstream.test.ts</files>
  <action>Implement a repo-local CLI that resolves the latest upstream default-branch commit, compares it to tracked state, updates tracked state when needed, and exposes machine-readable plus human-readable outputs for CI. Design it so the workflow can run verification before publish; the CLI should never itself push directly. It should clearly distinguish no-op, changed-but-unpublished, and blocked states.</action>
  <verify>bun run sync:upstream -- --root \"$PWD\" --upstream-repo-dir /Users/peterryszkiewicz/Repos/open-links --format json && bun run typecheck</verify>
  <done>The repo has one deterministic entrypoint for daily upstream sync preparation and no-op reporting.</done>
</task>

<task type="auto">
  <name>Task 3: Add the daily upstream-sync workflow and operator docs</name>
  <files>.github/workflows/upstream-sync.yml, README.md, package.json</files>
  <action>Add the daily scheduled workflow that checks out this repo plus upstream `open-links`, runs the sync CLI, runs the current verification gate, then commits and pushes one consolidated sync commit to `main` only when the sync result is changed and verification passed. Set git bot identity, keep the workflow no-op cleanly when upstream has not changed, and document the operator-facing behavior in the README.</action>
  <verify>bun run check && bun run validate</verify>
  <done>A daily workflow can update this repo against upstream `open-links` directly on `main` without manual intervention and without publishing unreleasable state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun test scripts/sync-upstream.test.ts` passes
- [ ] `bun run typecheck` passes
- [ ] `bun run check` passes
- [ ] `bun run validate` passes
- [ ] A no-op sync run reports that tracked upstream state already matches the latest upstream commit
- [ ] A changed sync run updates tracked upstream state but does not push before verification succeeds
- [ ] The workflow publishes one consolidated commit at most when upstream has moved
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo can sync itself to the latest upstream `open-links` commit directly on `main` through one deterministic daily workflow
</success_criteria>

<output>
After completion, create `.planning/phases/05-autonomous-upstream-sync-and-release-ops/05-01-SUMMARY.md`
</output>
