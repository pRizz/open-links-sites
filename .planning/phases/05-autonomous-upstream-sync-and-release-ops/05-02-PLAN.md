---
phase: 05-autonomous-upstream-sync-and-release-ops
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - README.md
  - .github/workflows/deploy.yml
  - package.json
  - scripts/build-site.ts
  - scripts/deploy-pages-plan.ts
  - scripts/deploy-pages-plan.test.ts
  - scripts/lib/deploy/pages-summary.ts
  - scripts/lib/release-ops/upstream-state.ts
  - scripts/lib/release-ops/deploy-context.ts
  - scripts/lib/release-ops/nightly-deploy.ts
autonomous: true
requirements:
  - DEPL-03
  - AUTO-01
user_setup: []
must_haves:
  truths:
    - Push, manual, and nightly deploys all build against the pinned upstream `open-links` revision recorded in this repo.
    - Nightly automation acts as a no-op-capable deploy backstop for current `main`, not a competing primary deploy path.
    - Deploy decisions remain driven by the final Pages artifact diff against the live `deploy-manifest.json`.
  artifacts:
    - .github/workflows/deploy.yml
    - scripts/lib/release-ops/upstream-state.ts
    - scripts/lib/release-ops/deploy-context.ts
    - scripts/lib/release-ops/nightly-deploy.ts
    - scripts/lib/deploy/pages-summary.ts
  key_links:
    - scripts/lib/release-ops/upstream-state.ts exposes the pinned upstream ref used by deploy workflows.
    - .github/workflows/deploy.yml resolves push vs nightly mode while keeping one centralized Pages deploy path.
    - scripts/lib/deploy/pages-summary.ts and scripts/deploy-pages-plan.ts surface concise no-op vs deploy summaries for nightly backstop runs.
---

<objective>
Implement the nightly delta-detection and deployment backstop on top of the new pinned-upstream contract.

Purpose: Ensure the live GitHub Pages site eventually converges to the current validated `main`, even if an earlier push-triggered deploy was missed or failed.
Output: A deploy workflow that can run on push, manual dispatch, and nightly schedule; always builds against the pinned upstream ref; and deploys only when the final Pages artifact differs from the live site.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-autonomous-upstream-sync-and-release-ops/05-CONTEXT.md
@.planning/phases/05-autonomous-upstream-sync-and-release-ops/05-RESEARCH.md
@.planning/phases/05-autonomous-upstream-sync-and-release-ops/05-01-PLAN.md
@.github/workflows/deploy.yml
@scripts/build-site.ts
@scripts/deploy-pages-plan.ts
@scripts/lib/deploy/pages-artifact.ts
@scripts/lib/deploy/pages-plan.ts
@scripts/lib/deploy/pages-summary.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread the pinned upstream ref through deploy context resolution</name>
  <files>scripts/lib/release-ops/upstream-state.ts, scripts/lib/release-ops/deploy-context.ts, .github/workflows/deploy.yml, scripts/build-site.ts</files>
  <action>Update the deploy path so CI no longer builds against floating upstream HEAD. Resolve the pinned upstream ref from the tracked upstream state file, expose it as workflow/build context, and make both push-triggered and nightly deploy builds use that exact upstream checkout. Preserve the existing operator-facing build behavior; this should be a CI truthfulness fix, not a new product surface.</action>
  <verify>bun run typecheck && bun test scripts/deploy-pages-plan.test.ts</verify>
  <done>All deploy builds are tied to a source-controlled upstream revision, so daily sync commits actually control what gets rendered and deployed.</done>
</task>

<task type="auto">
  <name>Task 2: Add nightly deploy-backstop mode to the centralized Pages workflow</name>
  <files>.github/workflows/deploy.yml, scripts/lib/release-ops/deploy-context.ts, scripts/lib/release-ops/nightly-deploy.ts, package.json</files>
  <action>Extend the existing deploy workflow so it can run nightly on the latest commit on `main` in addition to push/manual events. In nightly mode, build current `main` against the pinned upstream ref, reuse the existing Pages artifact planning flow, no-op cleanly when the live manifest already matches, and deploy only when the artifact differs. Keep push-triggered CI as the primary path; nightly exists only as a recovery/backstop mechanism.</action>
  <verify>bun run build:site -- --root \"$PWD\" && bun run deploy:pages:plan -- --site-dir generated/site --public-origin https://example.com/open-links-sites</verify>
  <done>The repo has a nightly deployment backstop that can converge the live Pages site to current `main` without creating a second deployment system.</done>
</task>

<task type="auto">
  <name>Task 3: Add concise nightly no-op/deploy summaries and docs</name>
  <files>README.md, scripts/deploy-pages-plan.ts, scripts/deploy-pages-plan.test.ts, scripts/lib/deploy/pages-summary.ts, .github/workflows/deploy.yml</files>
  <action>Make nightly behavior easy to read: summaries should distinguish push vs nightly mode, report pinned upstream ref, and clearly show no-op vs deploy outcome. Add tests or regression coverage for the summary and planning surface, and document what a normal nightly no-op run looks like versus a catch-up deploy.</action>
  <verify>bun test scripts/deploy-pages-plan.test.ts && bun run check</verify>
  <done>The nightly backstop is operationally understandable and reuses the same artifact-based truth as the primary deploy path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun test scripts/deploy-pages-plan.test.ts` passes
- [ ] `bun run typecheck` passes
- [ ] `bun run check` passes
- [ ] Push-triggered deploy builds use the pinned upstream ref from source control
- [ ] Nightly mode no-ops cleanly when the live Pages artifact already matches current `main`
- [ ] Nightly mode deploys when the live Pages artifact lags behind current `main`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo can automatically backstop GitHub Pages deployment nightly while keeping push-triggered deploys as the primary path
</success_criteria>

<output>
After completion, create `.planning/phases/05-autonomous-upstream-sync-and-release-ops/05-02-SUMMARY.md`
</output>
