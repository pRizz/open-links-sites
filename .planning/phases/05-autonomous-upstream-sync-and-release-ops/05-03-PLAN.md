---
phase: 05-autonomous-upstream-sync-and-release-ops
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - README.md
  - package.json
  - .github/workflows/upstream-sync.yml
  - .github/workflows/deploy.yml
  - scripts/release-verify.ts
  - scripts/release-verify.test.ts
  - scripts/lib/release-ops/release-verify.ts
  - scripts/lib/release-ops/smoke-check.ts
  - scripts/lib/release-ops/workflow-summary.ts
  - scripts/lib/release-ops/release-summary.ts
autonomous: true
requirements:
  - AUTO-02
  - DEPL-03
user_setup: []
must_haves:
  truths:
    - Daily sync and deploy workflows fail fast with concise, stage-based summaries and deterministic non-overlap.
    - Lightweight operational smoke checks run after release-critical stages without turning into a second full test suite.
    - Recovery remains fix-forward on `main`; no automatic rollback path is added.
  artifacts:
    - scripts/release-verify.ts
    - scripts/lib/release-ops/release-verify.ts
    - scripts/lib/release-ops/smoke-check.ts
    - scripts/lib/release-ops/workflow-summary.ts
    - .github/workflows/upstream-sync.yml
    - .github/workflows/deploy.yml
  key_links:
    - scripts/lib/release-ops/release-verify.ts owns the shared verification bundle reused by daily sync and release workflows.
    - scripts/lib/release-ops/smoke-check.ts adds the lightweight post-build or post-deploy operational checks required by Phase 5.
    - scripts/lib/release-ops/workflow-summary.ts standardizes concise no-op, success, and failure reporting across workflows.
---

<objective>
Finish Phase 5 by hardening release operations around summaries, smoke checks, and deterministic workflow guardrails.

Purpose: Turn the new daily sync and nightly deploy paths into predictable, debuggable automation instead of a loose collection of scheduled jobs.
Output: Shared release verification/smoke-check helpers, stage-based workflow summaries with remediation targets, and concurrency/guardrail wiring that keeps relevant workflows from overlapping or publishing unreleasable state.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-autonomous-upstream-sync-and-release-ops/05-CONTEXT.md
@.planning/phases/05-autonomous-upstream-sync-and-release-ops/05-RESEARCH.md
@.planning/phases/05-autonomous-upstream-sync-and-release-ops/05-01-PLAN.md
@.planning/phases/05-autonomous-upstream-sync-and-release-ops/05-02-PLAN.md
@.github/workflows/upstream-sync.yml
@.github/workflows/deploy.yml
@scripts/lib/deploy/pages-summary.ts
@scripts/build-site.ts
@scripts/deploy-pages-plan.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared release verification and lightweight smoke checks</name>
  <files>package.json, scripts/release-verify.ts, scripts/release-verify.test.ts, scripts/lib/release-ops/release-verify.ts, scripts/lib/release-ops/smoke-check.ts</files>
  <action>Add a shared release verification entrypoint that both daily sync and deploy workflows can call. It should bundle the existing repo checks with a Pages-ready site build, artifact planning, and a small set of smoke checks such as root output presence, deploy-manifest generation, and representative route verification. Keep the smoke layer intentionally lightweight and deterministic.</action>
  <verify>bun run release:verify -- --root \"$PWD\" --public-origin https://example.com/open-links-sites && bun test scripts/release-verify.test.ts</verify>
  <done>The repo has one reusable release gate that proves “confidently releasable” in the sense required by the user’s fail-fast automation policy.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize actionable summaries and failure diagnostics</name>
  <files>scripts/lib/release-ops/workflow-summary.ts, scripts/lib/release-ops/release-summary.ts, .github/workflows/upstream-sync.yml, .github/workflows/deploy.yml, README.md</files>
  <action>Standardize stage-based workflow summaries across sync and deploy flows. Every run should clearly say no-op, changed, deployed, or failed; identify the failing stage; and name the next remediation target. Preserve concise default summaries, but upload useful diagnostics on failure so the operator can fix forward quickly.</action>
  <verify>bun run check && bun run validate</verify>
  <done>The operator can tell what happened and where to look next without reading raw logs first, while detailed diagnostics remain available on failure.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce deterministic workflow guardrails and regression coverage</name>
  <files>.github/workflows/upstream-sync.yml, .github/workflows/deploy.yml, scripts/release-verify.test.ts, README.md</files>
  <action>Finish the Phase 5 operational guardrails: ensure relevant workflows share deterministic non-overlap behavior, keep publish steps behind successful verification, and document the fix-forward posture explicitly. Add regression coverage for no-op sync, blocked sync, nightly no-op, and post-build smoke-check failure cases so automation behavior stays pinned.</action>
  <verify>bun test scripts/release-verify.test.ts && bun run check</verify>
  <done>The new automation flows are guarded against overlap, publish only from releasable states, and stay understandable when they fail fast.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun test scripts/release-verify.test.ts` passes
- [ ] `bun run release:verify -- --root "$PWD" --public-origin https://example.com/open-links-sites` passes
- [ ] `bun run check` passes
- [ ] `bun run validate` passes
- [ ] Daily sync and deploy workflows emit concise stage-based summaries
- [ ] Relevant workflows do not overlap unpredictably
- [ ] Smoke-check failures stop release-critical workflows before publish
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has fail-fast, deterministic, and debuggable release automation for upstream sync and nightly Pages convergence
</success_criteria>

<output>
After completion, create `.planning/phases/05-autonomous-upstream-sync-and-release-ops/05-03-SUMMARY.md`
</output>
