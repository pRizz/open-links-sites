---
phase: 03-import-and-enrichment-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - package.json
  - README.md
  - scripts/lib/person-contract.ts
  - scripts/lib/materialize-person.ts
  - scripts/materialize-person.ts
  - scripts/lib/import/upstream-open-links-runner.ts
  - scripts/lib/import/cache-layout.ts
  - scripts/lib/import/cache-sync.ts
  - scripts/lib/manage-person/import-person.ts
  - scripts/manage-person.test.ts
  - scripts/materialize-person.test.ts
autonomous: true
requirements:
  - IMPT-03
  - IMPT-04
user_setup: []
must_haves:
  truths:
    - Successful imports automatically run the person-level upstream enrichment/cache flow.
    - Stable enrichment/cache outputs are persisted in per-person source-controlled helper locations in this repo.
    - Re-running enrichment is incremental by default, with an explicit full-refresh path available on top of the same bridge.
  artifacts:
    - scripts/lib/import/upstream-open-links-runner.ts
    - scripts/lib/import/cache-layout.ts
    - scripts/lib/import/cache-sync.ts
    - scripts/lib/manage-person/import-person.ts
    - scripts/lib/materialize-person.ts
  key_links:
    - scripts/lib/import/upstream-open-links-runner.ts invokes upstream `open-links` scripts against a generated single-person workspace.
    - scripts/lib/import/cache-sync.ts copies stable cache artifacts back into the per-person helper layout.
    - scripts/lib/person-contract.ts and scripts/lib/materialize-person.ts define the helper-artifact paths and generated-workspace expectations needed for repeatable upstream runs.
---

<objective>
Bridge imported person data into upstream `open-links` enrichment and cache flows without turning this repo into a second runtime.

Purpose: Reuse upstream validation, enrichment, avatar sync, content-image sync, and public-rich cache logic against one person at a time while persisting per-person cache artifacts back into this repo.
Output: An upstream runner + cache-sync boundary that Phase 3 imports can call automatically after source intake.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-import-and-enrichment-pipeline/03-CONTEXT.md
@.planning/phases/03-import-and-enrichment-pipeline/03-RESEARCH.md
@.planning/phases/03-import-and-enrichment-pipeline/03-01-PLAN.md
@scripts/lib/materialize-person.ts
@scripts/materialize-person.ts
@/Users/peterryszkiewicz/Repos/open-links/scripts/enrich-rich-links.ts
@/Users/peterryszkiewicz/Repos/open-links/scripts/validate-data.ts
@/Users/peterryszkiewicz/Repos/open-links/scripts/sync-profile-avatar.ts
@/Users/peterryszkiewicz/Repos/open-links/scripts/sync-content-images.ts
@/Users/peterryszkiewicz/Repos/open-links/scripts/public-rich-sync.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Formalize the per-person cache/import helper layout and materialized workspace contract</name>
  <files>scripts/lib/person-contract.ts, scripts/lib/materialize-person.ts, scripts/materialize-person.ts, README.md</files>
  <action>Define the helper-artifact paths Phase 3 will rely on, such as per-person cache and import-report locations, without changing the canonical required source files. Update the materialization boundary so generated workspaces contain the exact inputs/outputs the upstream runner needs, and keep generated output disposable under `generated/<id>/`.</action>
  <verify>bun test scripts/materialize-person.test.ts --filter import-cache-layout</verify>
  <done>The repo has an explicit contract for where per-person import/cache artifacts live and how they map into generated workspaces.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the upstream `open-links` runner bridge</name>
  <files>scripts/lib/import/upstream-open-links-runner.ts, scripts/lib/manage-person/import-person.ts, package.json</files>
  <action>Implement a thin bridge that runs upstream script files against a materialized single-person workspace by setting `cwd` to `generated/<id>/` and passing any required path flags. Cover the minimum Phase 3 pipeline: validate data, rich enrichment, avatar sync when a real remote avatar exists, content-image sync, and public-rich sync when applicable. Keep the bridge explicit about which upstream steps ran and which were intentionally skipped.</action>
  <verify>bun test scripts/manage-person.test.ts --filter enrichment-bridge</verify>
  <done>A Phase 3 import can reuse upstream enrichment/cache tooling without cloning or mutating the source-of-truth tree directly.</done>
</task>

<task type="auto">
  <name>Task 3: Sync stable cache outputs back into per-person source control</name>
  <files>scripts/lib/import/cache-layout.ts, scripts/lib/import/cache-sync.ts, scripts/lib/manage-person/import-person.ts, scripts/manage-person.test.ts</files>
  <action>Copy stable upstream cache outputs back into per-person helper paths after an import/enrichment run, while leaving runtime-only overlays or ephemeral generated outputs out of source control. Make incremental refresh the default behavior for existing people, and expose an explicit full-refresh path that the skill can prompt for later.</action>
  <verify>bun test scripts/manage-person.test.ts --filter cache-sync && bun run typecheck</verify>
  <done>Per-person imported data now owns committed enrichment/cache artifacts in this repo, and reruns follow the agreed incremental-default contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun test scripts/manage-person.test.ts` passes
- [ ] `bun test scripts/materialize-person.test.ts` passes
- [ ] `bun run typecheck` passes
- [ ] Upstream bridge runs against a generated workspace, not the source tree
- [ ] Stable cache outputs are synchronized back into per-person helper paths
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Imported people can be enriched and cached through upstream `open-links` while keeping per-person cache artifacts in this repo
</success_criteria>

<output>
After completion, create `.planning/phases/03-import-and-enrichment-pipeline/03-02-SUMMARY.md`
</output>
