---
phase: 01-foundation-and-data-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - biome.json
  - README.md
  - schemas/person.schema.json
  - templates/default/person.json
  - templates/default/profile.json
  - templates/default/links.json
  - templates/default/site.json
  - scripts/lib/person-contract.ts
  - scripts/lib/person-contract.test.ts
  - people/.gitkeep
  - generated/.gitkeep
autonomous: true
requirements:
  - DATA-01
  - OPER-02
user_setup: []
must_haves:
  truths:
    - The repo defines one canonical per-person source contract with required files, naming rules, and asset isolation.
    - Default templates are complete enough to create a valid person source shape without hand-authoring every file from scratch.
    - The core contract is explicit in code and docs rather than implied by scattered scripts.
  artifacts:
    - schemas/person.schema.json
    - templates/default/person.json
    - templates/default/profile.json
    - templates/default/links.json
    - templates/default/site.json
    - scripts/lib/person-contract.ts
  key_links:
    - scripts/lib/person-contract.ts defines the required person files and directory rules used by later scaffold and validation commands.
    - templates/default/*.json align with the contract decisions captured in 01-CONTEXT.md.
---

<objective>
Establish the repository's source contract, starter templates, and tooling skeleton for person-managed OpenLinks sites.

Purpose: Give every later script one authoritative definition of what a person folder contains and how reusable defaults are structured.
Output: Package/tooling bootstrap, person contract helpers, canonical templates, and baseline repo directories.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-data-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-data-contracts/01-RESEARCH.md
@/Users/peterryszkiewicz/Repos/open-links/docs/data-model.md
@/Users/peterryszkiewicz/Repos/open-links/schema/profile.schema.json
@/Users/peterryszkiewicz/Repos/open-links/schema/links.schema.json
@/Users/peterryszkiewicz/Repos/open-links/schema/site.schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the repo runtime and canonical directory layout</name>
  <files>package.json, tsconfig.json, biome.json, README.md, people/.gitkeep, generated/.gitkeep</files>
  <action>Initialize the repo as a Bun + TypeScript tooling project aligned with upstream open-links. Add the baseline directories that later plans depend on, wire the initial scripts placeholders, and update README so the source-of-truth layout is discoverable from the repo root. Keep this step thin: do not implement validation or operator CRUD yet.</action>
  <verify>bun run typecheck</verify>
  <done>The repo has executable TypeScript tooling metadata, deterministic baseline directories, and a README that explains the Phase 1 source contract at a high level.</done>
</task>

<task type="auto">
  <name>Task 2: Codify the person contract and default templates</name>
  <files>schemas/person.schema.json, templates/default/person.json, templates/default/profile.json, templates/default/links.json, templates/default/site.json, scripts/lib/person-contract.ts</files>
  <action>Create the repo-local `person.json` schema plus a shared contract helper that defines required filenames, discovery rules, and rename invariants. Add fully populated default templates with explicit placeholder values and strong starter defaults, while keeping `profile.json`, `links.json`, and `site.json` as upstream-compatible as possible. If local asset references force a narrow temporary divergence from upstream URI-only rules, isolate that divergence and document it clearly in the contract helper.</action>
  <verify>bun test scripts/lib/person-contract.test.ts</verify>
  <done>The repo has one authoritative person contract, the default templates reflect the locked Phase 1 decisions, and placeholder conventions are explicit rather than ad hoc.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract-level tests and invariants for later plans</name>
  <files>scripts/lib/person-contract.test.ts, scripts/lib/person-contract.ts</files>
  <action>Write focused tests that prove the contract helper exposes the required file list, naming rules, and default template completeness expected by later scaffold and validation commands. Use these tests to pin the intended source contract before any CLI behavior is added in Plans 02 and 03.</action>
  <verify>bun test scripts/lib/person-contract.test.ts && bun run typecheck</verify>
  <done>Later plans can import one tested contract module instead of re-deriving filenames, placeholders, or directory rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun test scripts/lib/person-contract.test.ts` passes
- [ ] `bun run typecheck` passes
- [ ] Template files and contract helper agree on required filenames and placeholder conventions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 has a single documented source contract and reusable default templates for a person folder
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-contracts/01-01-SUMMARY.md`
</output>
