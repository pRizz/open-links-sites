---
phase: 01-foundation-and-data-contracts
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - scripts/scaffold-person.ts
  - scripts/materialize-person.ts
  - scripts/lib/fill-templates.ts
  - scripts/lib/materialize-person.ts
  - scripts/scaffold-person.test.ts
  - scripts/materialize-person.test.ts
  - README.md
  - generated/.gitkeep
autonomous: true
requirements:
  - OPER-02
  - DATA-01
user_setup: []
must_haves:
  truths:
    - A low-level scaffold command can create a complete valid person folder from defaults without hand-authoring each JSON file.
    - Materialization produces a disposable single-person workspace that preserves the source contract and keeps generated output out of the source tree.
    - Scaffolded output can pass the Phase 1 validator with only explicit placeholder values remaining for later editing.
  artifacts:
    - scripts/scaffold-person.ts
    - scripts/materialize-person.ts
    - scripts/lib/fill-templates.ts
    - scripts/lib/materialize-person.ts
    - scripts/scaffold-person.test.ts
    - scripts/materialize-person.test.ts
  key_links:
    - scripts/scaffold-person.ts fills templates defined in 01-01 and validates them using the pipeline from 01-02.
    - scripts/materialize-person.ts converts one source-of-truth person folder into a generated workspace for future upstream build integration.
---

<objective>
Add the low-level scaffold and materialization utilities that make the Phase 1 contract usable by later CRUD and build phases.

Purpose: Turn the static contract into runnable foundation tooling without yet implementing the richer guided operator experience from Phase 2.
Output: A deterministic scaffold command, a generated single-person workspace boundary, and tests/docs for the scaffold -> validate -> materialize flow.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-data-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-data-contracts/01-RESEARCH.md
@.planning/phases/01-foundation-and-data-contracts/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-data-contracts/01-02-SUMMARY.md
@/Users/peterryszkiewicz/Repos/open-links/src/lib/content/load-content.ts
@/Users/peterryszkiewicz/Repos/open-links/docs/deployment.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic scaffolding from the default templates</name>
  <files>scripts/scaffold-person.ts, scripts/lib/fill-templates.ts, README.md</files>
  <action>Create a non-interactive scaffold entrypoint that fills the default templates with stable placeholder or user-provided values, creates `people/<id>/` with the required files, and keeps folder/id alignment explicit. This is foundation work only: the polished guided CRUD experience belongs to Phase 2, so keep inputs simple and deterministic rather than interactive or crawler-driven.</action>
  <verify>bun run scaffold:person -- --id fixture-user --name \"Fixture User\"</verify>
  <done>The repo can generate a complete valid person folder from the Phase 1 templates without manual file creation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the generated single-person workspace boundary</name>
  <files>scripts/materialize-person.ts, scripts/lib/materialize-person.ts, generated/.gitkeep</files>
  <action>Materialize one person's source folder into a disposable generated workspace that contains the public `data/*.json` trio and staged assets for future upstream build/deploy steps. Keep generated output clearly out of the source-of-truth tree, and document any narrow translation required for local asset references so later phases can either preserve or remove that boundary cleanly.</action>
  <verify>bun run materialize:person -- --id fixture-user</verify>
  <done>There is a repeatable generated workspace per person that later phases can hand to upstream `open-links` tooling without mutating source files in place.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the end-to-end foundation flow</name>
  <files>scripts/scaffold-person.test.ts, scripts/materialize-person.test.ts, README.md</files>
  <action>Add focused tests and usage docs that prove the intended foundation flow: scaffold a person, validate the folder, then materialize a generated workspace. The tests should cover placeholder handling, rename-safe ID/folder rules, and generated-output isolation so future Phase 2/3 work builds on a stable base instead of implicit assumptions.</action>
  <verify>bun test scripts/scaffold-person.test.ts scripts/materialize-person.test.ts && bun run validate</verify>
  <done>The foundation flow is reproducible, documented, and guarded by tests before richer CRUD/import behavior is introduced in later phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun test scripts/scaffold-person.test.ts scripts/materialize-person.test.ts` passes
- [ ] `bun run typecheck` passes
- [ ] Scaffolded output passes `bun run validate`
- [ ] Materialized output stays outside the source-of-truth `people/<id>/` tree
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a usable Phase 1 foundation flow: scaffold -> validate -> materialize
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-contracts/01-03-SUMMARY.md`
</output>
