---
phase: 01-foundation-and-data-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - scripts/validate.ts
  - scripts/lib/person-discovery.ts
  - scripts/lib/validate-person.ts
  - scripts/lib/validation-output.ts
  - scripts/validate.test.ts
  - schemas/person.schema.json
  - README.md
autonomous: true
requirements:
  - DATA-02
  - DATA-03
user_setup: []
must_haves:
  truths:
    - Validation fails hard on missing files, schema violations, folder-id mismatches, and asset-isolation breaches.
    - Disabled people still validate against the same canonical source contract as enabled people.
    - Validation output is usable by both humans and automation, including actionable problems, warnings, and suggestions.
  artifacts:
    - scripts/validate.ts
    - scripts/lib/person-discovery.ts
    - scripts/lib/validate-person.ts
    - scripts/lib/validation-output.ts
    - scripts/validate.test.ts
  key_links:
    - scripts/validate.ts uses the contract introduced in 01-01 instead of re-deriving file or folder rules.
    - Validation output distinguishes hard structural failures from softer placeholder or remediation guidance.
---

<objective>
Implement the structural and schema validation pipeline for per-person source data.

Purpose: Enforce the Phase 1 contract before later scaffold, import, and deploy workflows depend on it.
Output: A human/json validation CLI, shared discovery + validation helpers, and automated coverage for valid and invalid person folders.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-data-contracts/01-CONTEXT.md
@.planning/phases/01-foundation-and-data-contracts/01-RESEARCH.md
@.planning/phases/01-foundation-and-data-contracts/01-01-SUMMARY.md
@/Users/peterryszkiewicz/Repos/open-links/scripts/validate-data.ts
@/Users/peterryszkiewicz/Repos/open-links/schema/profile.schema.json
@/Users/peterryszkiewicz/Repos/open-links/schema/links.schema.json
@/Users/peterryszkiewicz/Repos/open-links/schema/site.schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build person discovery and structural validation</name>
  <files>scripts/lib/person-discovery.ts, scripts/lib/validate-person.ts, schemas/person.schema.json</files>
  <action>Implement deterministic discovery by scanning `people/*/person.json`, then enforce the canonical folder contract: required files, folder-name-to-`person.id` alignment, required `assets/` directory, and no cross-person asset leakage. Treat disabled people as first-class inputs that still must satisfy the same structure. Reuse the tested contract helper from Plan 01 rather than copying invariants into this layer.</action>
  <verify>bun test scripts/validate.test.ts --filter structure</verify>
  <done>Discovery and structural validation correctly accept valid person folders and hard-fail on naming, file, or asset-isolation violations.</done>
</task>

<task type="auto">
  <name>Task 2: Add upstream-compatible content validation and remediation output</name>
  <files>scripts/lib/validate-person.ts, scripts/lib/validation-output.ts, scripts/validate.ts</files>
  <action>Validate `profile.json`, `links.json`, and `site.json` against upstream-compatible schemas while preserving the local control-repo rules from Phase 1. Emit both human-readable and JSON output with problems, warnings, and suggestions so CI and future automation can remediate issues. Keep placeholder values valid but visible in output rather than silently normalizing them away.</action>
  <verify>bun run validate -- --format json && bun run validate</verify>
  <done>The repo has one validation entrypoint that can be used locally and in CI, with clear machine-readable output and actionable operator guidance.</done>
</task>

<task type="auto">
  <name>Task 3: Cover valid and invalid fixtures end-to-end</name>
  <files>scripts/validate.test.ts, people/, templates/default/</files>
  <action>Add focused tests and fixtures that prove the validation pipeline handles valid defaults, folder/id mismatches, missing required files, disabled-person handling, and asset-directory enforcement. Prefer realistic per-person fixture folders over isolated unit stubs so the validator is exercised against the same layout the repo will actually use.</action>
  <verify>bun test scripts/validate.test.ts && bun run typecheck</verify>
  <done>The validation pipeline is pinned by realistic fixtures and cannot regress silently when later CRUD/import plans begin writing person folders.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun test scripts/validate.test.ts` passes
- [ ] `bun run typecheck` passes
- [ ] `bun run validate` produces human-readable output
- [ ] `bun run validate -- --format json` produces machine-readable output with actionable issues
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo can discover and validate person folders with hard structural guarantees and automation-friendly output
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-data-contracts/01-02-SUMMARY.md`
</output>
