---
phase: 04-selective-multi-site-build-and-deploy
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - package.json
  - README.md
  - .github/workflows/deploy.yml
  - scripts/build-site.ts
  - scripts/deploy-pages-plan.ts
  - scripts/deploy-pages-plan.test.ts
  - scripts/lib/deploy/pages-artifact.ts
  - scripts/lib/deploy/pages-plan.ts
  - scripts/lib/deploy/pages-summary.ts
autonomous: true
requirements:
  - DEPL-01
  - DEPL-02
user_setup: []
must_haves:
  truths:
    - `generated/site/` is finalized into a Pages-ready artifact with manifest-driven no-op detection.
    - CI runs on build-relevant pushes, builds either selectively or fully, and fails the whole run if any targeted person build fails.
    - CI summaries stay concise and report build mode, built people, and deploy/artifact outcome.
  artifacts:
    - .github/workflows/deploy.yml
    - scripts/deploy-pages-plan.ts
    - scripts/lib/deploy/pages-artifact.ts
    - scripts/lib/deploy/pages-plan.ts
    - scripts/lib/deploy/pages-summary.ts
  key_links:
    - scripts/lib/deploy/pages-artifact.ts finalizes `generated/site/` into the exact Pages artifact boundary used by CI.
    - scripts/lib/deploy/pages-plan.ts and scripts/deploy-pages-plan.ts compare the local artifact to the live Pages manifest and decide deploy vs no-op.
    - .github/workflows/deploy.yml wires change detection, selective/full build selection, Pages artifact upload, deployment, and concise summaries into one centralized workflow.
---

<objective>
Finish Phase 4 by wiring Pages artifact finalization and centralized CI deployment on top of the new build foundation.

Purpose: Turn the local selective/full build flow into a fail-fast GitHub Pages deployment pipeline that only publishes when the final artifact actually changed.
Output: A push-triggered GitHub Actions workflow that builds `generated/site/`, finalizes it into a Pages-ready artifact, compares it to the live manifest, and deploys with concise summaries and failure diagnostics.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-selective-multi-site-build-and-deploy/04-CONTEXT.md
@.planning/phases/04-selective-multi-site-build-and-deploy/04-RESEARCH.md
@.planning/phases/04-selective-multi-site-build-and-deploy/04-01-PLAN.md
@.planning/phases/04-selective-multi-site-build-and-deploy/04-02-PLAN.md
@/Users/peterryszkiewicz/Repos/open-links/.github/workflows/deploy-pages.yml
@/Users/peterryszkiewicz/Repos/open-links/scripts/deploy/plan-pages.ts
@/Users/peterryszkiewicz/Repos/open-links/scripts/lib/deploy-artifact.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize the merged site into a Pages-ready artifact and deploy plan</name>
  <files>scripts/lib/deploy/pages-artifact.ts, scripts/lib/deploy/pages-plan.ts, scripts/deploy-pages-plan.ts, scripts/deploy-pages-plan.test.ts, scripts/build-site.ts</files>
  <action>Implement the final artifact boundary for `generated/site/`: add `.nojekyll` and any other required Pages metadata, write `deploy-manifest.json`, and compare the local manifest to the live Pages manifest to decide whether deployment is needed. Reuse the upstream deploy-manifest pattern as closely as practical so Phase 5 automation can stay familiar and predictable.</action>
  <verify>bun test scripts/deploy-pages-plan.test.ts && bun run typecheck</verify>
  <done>The repo can prove whether a built central artifact differs from the live Pages site before attempting deployment.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the centralized GitHub Pages workflow</name>
  <files>.github/workflows/deploy.yml, package.json, scripts/build-site.ts, scripts/changed-people.ts</files>
  <action>Add the GitHub Actions workflow for Phase 4. It should trigger on successful pushes that touch build-relevant files, resolve selective vs full mode, run the appropriate site build, finalize the Pages artifact, and deploy only when the manifest plan reports a change. Preserve the user’s fail-fast rule: any targeted person build failure fails the whole workflow.</action>
  <verify>bun run typecheck && bun run validate</verify>
  <done>A single centralized workflow can build and deploy the path-based multi-site output to GitHub Pages from this repo.</done>
</task>

<task type="auto">
  <name>Task 3: Add concise CI summaries and failure diagnostics</name>
  <files>scripts/lib/deploy/pages-summary.ts, .github/workflows/deploy.yml, README.md, scripts/deploy-pages-plan.test.ts</files>
  <action>Add concise workflow summaries that report build mode, built people, deploy/no-op result, and artifact hash or destination when relevant. Upload useful diagnostics on failure, but keep the default summary short. Update docs so the operator knows what a normal selective run, full rebuild, and Pages no-op look like.</action>
  <verify>bun test scripts/deploy-pages-plan.test.ts && bun run check</verify>
  <done>The deployment workflow is understandable in normal operation and debuggable when it fails fast.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun test scripts/deploy-pages-plan.test.ts` passes
- [ ] `bun run typecheck` passes
- [ ] `bun run validate` passes
- [ ] CI can choose selective vs full build mode and still produce a full Pages-ready artifact
- [ ] Deploy is skipped cleanly when the live Pages manifest already matches the built artifact
- [ ] Any targeted person build failure fails the workflow
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo can build, finalize, and centrally deploy path-based multi-site output to GitHub Pages with selective rebuild optimization
</success_criteria>

<output>
After completion, create `.planning/phases/04-selective-multi-site-build-and-deploy/04-03-SUMMARY.md`
</output>
