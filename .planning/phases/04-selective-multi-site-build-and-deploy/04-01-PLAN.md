---
phase: 04-selective-multi-site-build-and-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - README.md
  - scripts/build-person-site.ts
  - scripts/build-site.ts
  - scripts/build-site.test.ts
  - scripts/lib/build/site-layout.ts
  - scripts/lib/build/build-person-site.ts
  - scripts/lib/build/build-site.ts
  - src/landing/App.tsx
  - src/landing/main.tsx
  - vite.landing.config.ts
  - landing.html
  - "/Users/peterryszkiewicz/Repos/open-links/vite.config.ts"
  - "/Users/peterryszkiewicz/Repos/open-links/src/lib/content/load-content.ts"
autonomous: true
requirements:
  - DEPL-01
user_setup: []
must_haves:
  truths:
    - Upstream `open-links` remains the canonical per-person renderer.
    - Each enabled person can build into a self-contained `generated/site/<id>/` route with a per-person base path.
    - Root `/` is generated locally in this repo as a minimal SolidJS landing page.
  artifacts:
    - scripts/lib/build/build-person-site.ts
    - scripts/lib/build/build-site.ts
    - scripts/build-person-site.ts
    - scripts/build-site.ts
    - src/landing/App.tsx
    - vite.landing.config.ts
  key_links:
    - scripts/lib/build/build-person-site.ts materializes a person and invokes the upstream renderer through the new workspace-aware build seam.
    - "/Users/peterryszkiewicz/Repos/open-links/vite.config.ts" and "/Users/peterryszkiewicz/Repos/open-links/src/lib/content/load-content.ts" are the narrow upstream generalization needed to build from external per-person workspaces.
    - src/landing/App.tsx and vite.landing.config.ts own only the root landing page, not the per-person app runtime.
---

<objective>
Implement the Phase 4 build foundation: one-person upstream-backed site generation plus the local root landing page.

Purpose: Establish a truthful build boundary where this repo orchestrates per-person site assembly while upstream `open-links` still owns rendering.
Output: A repeatable build flow that can generate a full central site from scratch under `generated/site/`, with per-person self-contained outputs and a local Solid landing page at `/`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-selective-multi-site-build-and-deploy/04-CONTEXT.md
@.planning/phases/04-selective-multi-site-build-and-deploy/04-RESEARCH.md
@.planning/phases/03-import-and-enrichment-pipeline/03-VERIFICATION.md
@scripts/lib/materialize-person.ts
@scripts/lib/import/cache-layout.ts
@README.md
@/Users/peterryszkiewicz/Repos/open-links/vite.config.ts
@/Users/peterryszkiewicz/Repos/open-links/src/lib/content/load-content.ts
@/Users/peterryszkiewicz/Repos/open-links/scripts/deploy/build.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the narrow upstream build seam for external person workspaces</name>
  <files>/Users/peterryszkiewicz/Repos/open-links/vite.config.ts, /Users/peterryszkiewicz/Repos/open-links/src/lib/content/load-content.ts</files>
  <action>Generalize the upstream frontend build so it can read `data/`, generated metadata/cache files, and public assets from a materialized person workspace instead of only the upstream repo root. Keep the upstream default behavior unchanged when no external workspace is provided, and preserve `BASE_PATH` support for per-person route prefixes.</action>
  <verify>cd /Users/peterryszkiewicz/Repos/open-links && bun run typecheck && bun test</verify>
  <done>The upstream renderer can build a single person from external content roots without requiring repo-root file copying hacks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement per-person site build orchestration in this repo</name>
  <files>scripts/lib/build/site-layout.ts, scripts/lib/build/build-person-site.ts, scripts/lib/build/build-site.ts, scripts/build-person-site.ts, scripts/build-site.ts</files>
  <action>Add local build commands that materialize a person, invoke the upstream renderer through the new build seam, and write self-contained output into `generated/site/<id>/`. Support full-build mode over all enabled people, ensure disabled or archived people are omitted from output, and keep `generated/site/` as the only Pages-ready merged artifact directory.</action>
  <verify>bun test scripts/build-site.test.ts --filter person-build && bun run typecheck</verify>
  <done>This repo can build the central multi-site output from scratch while keeping per-person rendering delegated to upstream `open-links`.</done>
</task>

<task type="auto">
  <name>Task 3: Add the local SolidJS landing page and document the full-build flow</name>
  <files>package.json, README.md, src/landing/App.tsx, src/landing/main.tsx, vite.landing.config.ts, landing.html, scripts/build-site.test.ts</files>
  <action>Add the minimal local SolidJS landing page for `/`, keep it separate from per-person bundles, and wire it into the full build so root output lands in `generated/site/`. Update docs and tests to prove the root landing page, full site build, and per-person route layout work together without introducing shared-runtime coupling.</action>
  <verify>bun test scripts/build-site.test.ts --filter landing-page && bun run validate</verify>
  <done>The generated site includes the requested root landing page plus self-contained per-person routes, and the build flow is documented for operators.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd /Users/peterryszkiewicz/Repos/open-links && bun run typecheck && bun test` passes
- [ ] `bun test scripts/build-site.test.ts` passes
- [ ] `bun run typecheck` passes
- [ ] Full build writes the merged Pages-ready site into `generated/site/`
- [ ] Each enabled person builds into a self-contained `generated/site/<id>/` route
- [ ] Root `/` is produced by the local Solid landing app
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo can build a full path-based multi-site output from scratch while keeping `open-links` as the canonical per-person renderer
</success_criteria>

<output>
After completion, create `.planning/phases/04-selective-multi-site-build-and-deploy/04-01-SUMMARY.md`
</output>
