---
phase: 04-selective-multi-site-build-and-deploy
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - package.json
  - README.md
  - scripts/build-site.ts
  - scripts/changed-people.ts
  - scripts/build-site.test.ts
  - scripts/changed-people.test.ts
  - scripts/lib/build/change-detection.ts
  - scripts/lib/build/restore-live-site.ts
  - scripts/lib/build/selective-build.ts
autonomous: true
requirements:
  - DEPL-02
user_setup: []
must_haves:
  truths:
    - Single-person changes rebuild only the affected people unless a widening rule forces a full rebuild.
    - Shared repo/runtime/template/schema changes widen to a full rebuild of all enabled people.
    - Selective builds restore a full base snapshot before overlaying changed people, and fall back to a full rebuild when confidence is low.
  artifacts:
    - scripts/changed-people.ts
    - scripts/lib/build/change-detection.ts
    - scripts/lib/build/restore-live-site.ts
    - scripts/lib/build/selective-build.ts
    - scripts/build-site.ts
  key_links:
    - scripts/lib/build/change-detection.ts classifies file changes into targeted people vs full-rebuild triggers.
    - scripts/lib/build/restore-live-site.ts recovers the currently deployed Pages artifact as the base snapshot for selective merges.
    - scripts/lib/build/selective-build.ts overlays rebuilt people into the merged artifact while pruning removed routes.
---

<objective>
Implement the selective-build contract for centralized path-based output.

Purpose: Make the multi-site build cheap on person-only changes without lying about how GitHub Pages deployments work.
Output: A deterministic changed-person detector plus selective artifact-merge flow that restores the live site as a base snapshot, overlays rebuilt people, and widens to a full rebuild when needed.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-selective-multi-site-build-and-deploy/04-CONTEXT.md
@.planning/phases/04-selective-multi-site-build-and-deploy/04-RESEARCH.md
@.planning/phases/04-selective-multi-site-build-and-deploy/04-01-PLAN.md
@scripts/lib/materialize-person.ts
@scripts/lib/import/cache-layout.ts
@/Users/peterryszkiewicz/Repos/open-links/scripts/deploy/plan-pages.ts
@/Users/peterryszkiewicz/Repos/open-links/scripts/lib/deploy-artifact.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement change classification and changed-person detection</name>
  <files>scripts/changed-people.ts, scripts/changed-people.test.ts, scripts/lib/build/change-detection.ts, README.md</files>
  <action>Implement the deterministic change detector for Phase 4. Anything under `people/<id>/` should mark that person as changed, including helper cache/import artifacts. Shared repo code, templates, schemas, upstream-bridge files, landing-page files, or any uncertain classification should widen the build to all enabled people. Expose the result as both human-readable summary text and machine-readable JSON for CI consumption.</action>
  <verify>bun test scripts/changed-people.test.ts && bun run typecheck</verify>
  <done>The repo can reliably decide whether a push is a one-person rebuild, a targeted multi-person rebuild, or a full rebuild.</done>
</task>

<task type="auto">
  <name>Task 2: Restore the live Pages site and overlay targeted rebuilds</name>
  <files>scripts/lib/build/restore-live-site.ts, scripts/lib/build/selective-build.ts, scripts/build-site.ts, scripts/build-site.test.ts</files>
  <action>Add the selective-build merge path: restore the currently deployed Pages artifact into `generated/site/`, rebuild only the targeted people on top of it, and delete route directories for any targeted people that are now disabled or archived. If restoring the live site or proving the target set fails, widen to a full rebuild automatically instead of blocking deployment.</action>
  <verify>bun test scripts/build-site.test.ts --filter selective-merge</verify>
  <done>Selective builds produce a full Pages-ready artifact that preserves unchanged people and updates only the intended routes.</done>
</task>

<task type="auto">
  <name>Task 3: Prove widening, pruning, and fail-open behavior with regression coverage</name>
  <files>scripts/build-site.test.ts, scripts/changed-people.test.ts, README.md</files>
  <action>Add regression coverage for helper-artifact-only person changes, shared-code full-rebuild widening, failed live-site restore fallback, and targeted disable/archive pruning. Keep the docs explicit that selective builds are an optimization, not a different correctness path, and that low-confidence cases intentionally rebuild all enabled people.</action>
  <verify>bun test scripts/changed-people.test.ts && bun test scripts/build-site.test.ts && bun run validate</verify>
  <done>The selective-build rules are pinned tightly enough that CI can use them without hiding correctness failures behind optimization logic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun test scripts/changed-people.test.ts` passes
- [ ] `bun test scripts/build-site.test.ts` passes
- [ ] `bun run typecheck` passes
- [ ] Person-only changes stay targeted unless a widening rule applies
- [ ] Selective builds restore a base artifact before overlaying changed people
- [ ] Disable/archive changes remove stale route output from the merged artifact
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo can safely select changed people and assemble a full merged artifact without rebuilding everyone every time
</success_criteria>

<output>
After completion, create `.planning/phases/04-selective-multi-site-build-and-deploy/04-02-SUMMARY.md`
</output>
